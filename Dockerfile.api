FROM python:3.11-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml ./

RUN uv sync --frozen --no-install-project

COPY src ./src

ENV PYTHONPATH=/app

EXPOSE 8000

CMD ["uvicorn", "dev_workflow.api.server:app", "--host", "0.0.0.0", "--port", "8000"]
